<?xml version="1.0" encoding="utf-8"?>
<!-- Controller customizado do PlayerView (Media3): igual ao padrão, mas com o
     título logo acima da barra e os botões "espelhar"/"assistido" DENTRO da
     barra de baixo, ao lado de legenda/config (antes ficavam flutuando por cima). -->
<merge xmlns:android="http://schemas.android.com/apk/res/android">

  <View android:id="@id/exo_controls_background"
      android:layout_width="0dp"
      android:layout_height="0dp"
      android:background="@color/exo_black_opacity_60"/>

  <!-- Título + temporada/ep: logo acima da barra de progresso, parte do controller. -->
  <TextView android:id="@+id/wm_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="bottom|start"
      android:layout_marginStart="16dp"
      android:layout_marginEnd="16dp"
      android:layout_marginBottom="72dp"
      android:singleLine="true"
      android:ellipsize="end"
      android:textColor="#FFFFFF"
      android:textSize="14sp"
      android:shadowColor="#000000"
      android:shadowRadius="6"/>

  <FrameLayout android:id="@id/exo_bottom_bar"
      android:layout_width="match_parent"
      android:layout_height="@dimen/exo_styled_bottom_bar_height"
      android:layout_marginTop="@dimen/exo_styled_bottom_bar_margin_top"
      android:layout_gravity="bottom"
      android:background="@color/exo_bottom_bar_background"
      android:layoutDirection="ltr">

    <LinearLayout android:id="@id/exo_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="@dimen/exo_styled_bottom_bar_time_padding"
        android:paddingEnd="@dimen/exo_styled_bottom_bar_time_padding"
        android:layout_gravity="center_vertical|start"
        android:layoutDirection="ltr">

      <TextView android:id="@id/exo_position"
          style="@style/ExoStyledControls.TimeText.Position"/>

      <TextView
          style="@style/ExoStyledControls.TimeText.Separator"/>

      <TextView android:id="@id/exo_duration"
          style="@style/ExoStyledControls.TimeText.Duration"/>

    </LinearLayout>

    <LinearLayout android:id="@id/exo_basic_controls"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical|end"
        android:layoutDirection="ltr">

      <!-- Botões próprios: assistido + espelhar, na mesma barra da legenda/config. -->
      <ImageButton android:id="@+id/wm_watched"
          style="@style/ExoStyledControls.Button.Bottom"
          android:src="@drawable/ic_check_circle"
          android:contentDescription="Assistido"/>

      <ImageButton android:id="@+id/wm_cast"
          style="@style/ExoStyledControls.Button.Bottom"
          android:src="@drawable/ic_cast"
          android:contentDescription="Espelhar na TV"/>

      <ImageButton android:id="@id/exo_subtitle"
          style="@style/ExoStyledControls.Button.Bottom.CC"/>

      <ImageButton android:id="@id/exo_settings"
          style="@style/ExoStyledControls.Button.Bottom.Settings"/>

    </LinearLayout>

  </FrameLayout>

  <View android:id="@id/exo_progress_placeholder"
      android:layout_width="match_parent"
      android:layout_height="@dimen/exo_styled_progress_layout_height"
      android:layout_gravity="bottom"
      android:layout_marginBottom="@dimen/exo_styled_progress_margin_bottom"/>

  <LinearLayout
      android:id="@id/exo_center_controls"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:background="@android:color/transparent"
      android:gravity="center"
      android:padding="@dimen/exo_styled_controls_padding"
      android:clipToPadding="false"
      android:layoutDirection="ltr">

    <include layout="@layout/exo_player_control_rewind_button" />

    <ImageButton android:id="@id/exo_play_pause"
        style="@style/ExoStyledControls.Button.Center.PlayPause"/>

    <include layout="@layout/exo_player_control_ffwd_button" />

  </LinearLayout>

</merge>
